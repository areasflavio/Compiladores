begin

real x1;
real y1;
real y2;
real x2;

output("Digite o valor da posição X do primeiro ponto");
input(x1);
output("Digite o valor da posição Y do primeiro ponto");
input(y1);
output("Digite o valor da posição X do segundo ponto");
input(x2);
output("Digite o valor da posição Y do segundo ponto");
input(y2);

real res;

res = sqrt((x2 - x1)*(x2 - x1) + (y2 - y1)*(y2 - y1));

output(res);

end